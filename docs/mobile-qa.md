# Mobile QA

# Viewports
- [x] CSS includes a tablet breakpoint at `1280px`.
- [x] CSS includes a compact breakpoint at `920px`.
- [x] CSS includes a mobile breakpoint at `720px`.
- [x] Dashboard shell collapses from sidebar/workspace columns to one column.
- [x] Lower dashboard grid collapses to one column on mobile.
- [x] Connector rows collapse to one column on small screens.
- [x] Header, station detail header, simulator controls, and card heads stack vertically on small screens.

# Manual Screenshot Pass
- [ ] Capture `390x844` login screen.
- [ ] Capture `390x844` dashboard overview after login.
- [ ] Capture `390x844` simulator panel with scenario list.
- [ ] Capture `1440x1000` dashboard overview.
- [ ] Capture `1440x1000` station detail and connector drawer.

# Acceptance Criteria
- [ ] No text overlaps adjacent controls.
- [ ] Sidebar navigation remains reachable after collapse.
- [ ] Scenario chips keep readable labels.
- [ ] Connector rows do not force horizontal scrolling on phone width.
- [ ] Action errors are visible in the top bar.
- [ ] Empty states render in panels without data.
